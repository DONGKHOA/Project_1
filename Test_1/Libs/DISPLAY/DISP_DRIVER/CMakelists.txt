set(component_srcs "disp_spi.c")

idf_component_register(SRCS "${component_srcs}"
                       INCLUDE_DIRS "."
                       REQUIRES "freertos" "driver" "esp_err" "esp_log" DISP_SPI
                       )